/**
 * First we will load all of this project's JavaScript dependencies which
 * includes React and other helpers. It's a great starting point while
 * building robust, powerful web applications using React + Laravel.
 */

require('./bootstrap');

/**
 * Next, we will create a fresh React component instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */


//require('./components/Nuevocurso');
require('./components/Curso');
//require('./components/Micurso');
//require('./components/Videoapp');
//require('./components/Vercurso');
//require('./components/Verleccion');

/**
 * Librerias necesarias
 * php artisan ui react
 * npm install
 * npm install react-html-parser
 * npm install --save react-oembed-container
 * npm install --save react react-dom
 * npm install oembed-parser
 * npm i @ckeditor/ckeditor5-react
 * npm install --save @ckeditor/ckeditor5-build-classic
 * npm i pusher-js
 * npm i simple-peer
 *
 */